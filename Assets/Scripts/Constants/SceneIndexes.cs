
namespace OtakuGameJam.Constants
{
    /// <summary>
    /// This <b>enum</b> is used to store the index of the scenes in the build settings. <br />
    /// ⚠ Remember to update the enum when you add a new scene to the build settings. And to use the correct values found within the build settings.
    /// </summary>
    public enum SceneIndex
    {
        EmptyScene,
        MainMenu,
        Game,
        GameOver
    }
}